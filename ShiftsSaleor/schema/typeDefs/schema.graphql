type User {
  name: String!
}

type Query {
  getUser: String
}

type Mutation {
  """Create shift group. Requires permission MANAGE_STAFF"""
  userCreate(
    """Name of the user"""
    name: String
  ): User
}