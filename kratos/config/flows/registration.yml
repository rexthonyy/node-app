flow:
  request_url: /
  expires_at: 10000 # milliseconds
  active_method: password
  type: api
  state: active
  default_state: choose method
  ui:
    action: /
    method: post
    nodes:
      attributes:
        type: id
      group: none
      type: ui
      messages:
        context: current
        id: attr
        text: info
        type: ui
  internal_context: /